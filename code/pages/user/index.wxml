<!--pages/user/index.wxml-->
<view>
  <text>计划</text>
  <view wx:for="{{userList}}">
  {{item.startTime}}——{{item.name}}：{{item.status}}
</view>
<view><button bindtap='cleanList'>清空所有的记录</button></view>
</view>

